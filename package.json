{
  "name": "importer_script",
  "version": "1.0.0",
  "description": "Office attendance importer: read Excel, convert to JSON, POST to DDO++ API",
  "main": "scripts/attendance_import.js",
  "scripts": {
    "import": "node scripts/attendance_import.js"
  },
  "license": "ISC",
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
